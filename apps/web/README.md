# Web (frontend)

Frontend do Zeladoria Inteligente. Será implementado com **Next.js** + TypeScript.

Para criar o app Next.js aqui:

```bash
npm create next-app@latest . --typescript --eslint --tailwind --app --src-dir
```

Depois ajuste o `package.json` com os scripts reais (`dev`, `build`, `start`, `lint`).
